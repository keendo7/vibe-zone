<%= turbo_stream_action_tag(
  "append",
  target: "users",
  template: %(#{render @users}) 
) %>
<%= turbo_stream_action_tag(
  "replace",
  target: "pager",
  template: %(#{render "pager", pagy: @pagy})
) %>