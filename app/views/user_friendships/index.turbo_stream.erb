<%= turbo_stream_action_tag(
  "append",
  target: "friendships",
  template: %(#{render @friendships}) 
) %>
<%= turbo_stream_action_tag(
  "replace",
  target: "pager",
  template: %(#{render "pager", pagy: @pagy})
) %>
