<%= turbo_stream_action_tag(
  "append",
  target: "friendships",
  template: %(#{render @mutuals}) 
) %>
<%= turbo_stream_action_tag(
  "replace",
  target: "pager",
  template: %(#{render "pager_mutuals", pagy: @pagy})
) %>
