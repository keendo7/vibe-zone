<%= turbo_frame_tag :notifications, class: "d-flex flex-column align-items-center" do %>
  <%= render @notifications %>
<% end %>

<%= turbo_frame_tag :pagination, 
      src: notifications_path(format: :turbo_stream), 
      loading: :lazy  
%>
