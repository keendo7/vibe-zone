<%= turbo_stream_from current_user, "notifications_list" %>

<%= turbo_frame_tag :notifications do %>
  <%= render @notifications %>
<% end %>

<%= turbo_frame_tag :pagination, 
      src: notifications_path(format: :turbo_stream), 
      loading: :lazy  
%>
