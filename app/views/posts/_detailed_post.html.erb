<%= turbo_frame_tag dom_id(post) do %> 
  <li> <%= image_tag user_avatar(post.author, 50), class: "avatar" %> </li> 
  <li> <b> <%= post.author.full_name %> </b> </li>
  <li> added <%= time_ago_in_words(post.created_at) %> ago </li>
  <li> <%= post.content %> </li>
  <%= render partial: "likes/post_buttons", locals: { post: post } %>
  <% if post.author == current_user %>
    <%= button_to "Delete", post_path(post), method: :delete, data: { turbo: false } %>
  <% end %>
<% end %>
